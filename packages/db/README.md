# @agit/db — 마이그레이션

출처: `작성_05_AGIT_DB생성SQL_v0_1.md` 를 파일로 분리(내용 무변경). up↔down 7쌍 = 14개 파일.

## ⚠️ 실행은 R3 — 사람이 한다
규칙_04 ⑦ T-007(DB 마이그레이션) = **R3**. AI·CI가 단독 실행하지 않는다.
적용 순서는 001 → 007, 되돌리기는 역순. dry-run 먼저.

## 상태
- 문법: pglast v8.2 파싱 **14/14 OK · 총 131 statements** (2026-07-22)
- ⚠️ **[열림 C-DB-04]** 작성_05 문서에 기록된 증거는 `111 statements` 인데 같은 도구 같은 버전으로 재현하면 **131** 이 나온다. 숫자가 재현되지 않는다 → 사람 확인 필요.
- 런타임 실행: **미검증**. 실제 Postgres/Supabase에 적용한 로그가 있어야 G1 통과.
- 미결: C-DB-01(class_enrollments) · C-DB-02(users의 workspace_id 예외) — 실행 전 확정 권장.
- RLS **정책 본문**(008)은 아직 없다. 007은 `enable row level security` 까지만이다 → 정책 없이 활성화하면 접근이 전면 차단될 수 있다. 순서를 사람이 확인할 것.
